#!/bin/bash
# AgentisOrchestra -- User Flow E2E Test
# Tests: admin creates employee, employee uses all features, permissions enforced
# Requires: completed setup with at least 1 department and 1 agent
# Usage: ADMIN_EMAIL=x ADMIN_PASS=y ./tests/e2e-userflow.sh [base-url]
echo "Run this test against a live Orchestra instance with completed setup."
echo "See the inline version in the conversation for the full script."
